# Public URL the browser uses (goes through Vite dev proxy)
VITE_DIRECTUS_URL=/directus

# Real Directus server URL the Vite dev proxy forwards to
VITE_DIRECTUS_INTERNAL_URL=http://localhost:8055

# Optional: static token if collection is public or you use token auth
# VITE_DIRECTUS_TOKEN=
